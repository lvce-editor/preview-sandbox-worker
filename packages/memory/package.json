{
  "name": "@lvce-editor/preview-sandbox-worker-memory",
  "version": "0.0.0-dev",
  "type": "module",
  "scripts": {
    "measure": "cd ../build && npm run measure",
    "measure:visible": "cd ../build && npm run measure:visible"
  },
  "devDependencies": {
    "@types/node": "^22.14.1"
  }
}
